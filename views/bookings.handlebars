
<h1 class="title is-2" >Bookings</h1>

    <div class="notification is-danger is-light">
     
        {{!-- you can remove the button below if you want --}}
        <button class="delete"></button>
           {{messages.error1}}
    </div>
    <div class="notification is-success is-light">
        {{!-- you can remove the button below if you want --}}
      
        <button class="delete" ></button>
       
    </div>

    <div class="field">

        <label class="label" for="">Bookings for</label>
        <form action="/booking/:username" method="post">
        <input class="input is-normal is-2" type="text" placeholder="enter username" name="username" > 
    </div>
    <div class="field" >
        <input class="button is-info" type="submit" value="Search">
    </div>
</form>
<table class="table is-fullwidth" >
     {{#each tables}}
     
    <tr>
        <th>Table name</th>
        <th>Table capacity</th>
        <th>Booking size</th>
        <th>Booked by</th>
        <th>Contact number</th>
        <th></th>
    </tr>
    <tr>
        <td>{{this.table_name}}</td>
        <td>{{this.capacity}}</td>
        <td>{{this.number_of_people}}</td>
        <td>{{this.username}}</td>
        <td>{{this.contact_number}}</td>
        <td>
<form action="/cancel" method="post">
            <button class="button is-warning" >Cancel booking</button>
            </form>
        </td>
    </tr>
   
        {{/each}}
  
</table>
